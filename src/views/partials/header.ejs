<!DOCTYPE html>
<html lang="en" data-bs-theme="light"> <!-- toggle to "dark" if you prefer -->
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>

    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Bootstrap Icons -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css"
    />
  </head>

  <body class="d-flex flex-column min-vh-100 bg-body text-body">
<nav class="navbar navbar-expand-lg bg-body-tertiary shadow-sm mb-4">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/">Mini Message Board</a>
    <div class="d-flex align-items-center gap-2">
      <a href="/new" class="btn btn-primary">
        <i class="bi bi-plus-circle me-1"></i> New Message
      </a>

      <!-- Theme toggle button -->
      <button id="themeToggle" class="btn btn-outline-secondary" type="button">
        <i id="themeIcon" class="bi bi-moon"></i>
      </button>
    </div>
  </div>
</nav>
