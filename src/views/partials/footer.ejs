<footer class="mt-5 py-4 bg-body-tertiary border-top">
  <div class="container text-center text-muted small">
    <a
        href="https://github.com/furkanssarri"
        target="_blank"
        rel="noopener noreferrer"
        className="footer-link"
        aria-label="Open GitHub profile of furkanssarri in a new tab"
      >
        <i class="fa-brands fa-github"></i>
        furkanssarri
      </a>
       <%= new Date().getFullYear() %>  Mini Message Board
  </div>
</footer>
<script src="https://kit.fontawesome.com/a4cf3a1ade.js" crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
  (function () {
    const html = document.documentElement;
    const toggleBtn = document.getElementById("themeToggle");
    const icon = document.getElementById("themeIcon");

    // Load saved preference
    if (localStorage.getItem("theme")) {
      html.setAttribute("data-bs-theme", localStorage.getItem("theme"));
      icon.className = localStorage.getItem("theme") === "dark" ? "bi bi-sun" : "bi bi-moon";
    }

    toggleBtn.addEventListener("click", () => {
      const current = html.getAttribute("data-bs-theme");
      const next = current === "dark" ? "light" : "dark";
      html.setAttribute("data-bs-theme", next);
      localStorage.setItem("theme", next);
      icon.className = next === "dark" ? "bi bi-sun" : "bi bi-moon";
    });
  })();
</script>
</body>
</html>
